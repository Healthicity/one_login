source 'https://rubygems.org'

git_source(:github) { |repo| "https://github.com/#{repo}.git" }

gem 'http_api', github: 'Healthicity/http_api', tag: '1.2.0'

# Specify your gem's dependencies in one_login.gemspec
gemspec